{
  "name": "@cloudflare/puppeteer",
  "description": "Puppeteer for Cloudflare Workers",
  "version": "1.0.0-next",
  "license": "Apache-2.0",
  "type": "module",
  "files": [
    "lib/"
  ],
  "exports": {
    ".": {
      "default": "./lib/index.js"
    },
    "./internal/*": {
      "default": "./lib/submodules/puppeteer/packages/puppeteer-core/lib/esm/puppeteer/*"
    }
  },
  "scripts": {
    "patch": "cd ../.. && npm run patch:puppeteer",
    "build:puppeteer": "cd ../../submodules/puppeteer/packages/puppeteer-core && npx tsc -p src/tsconfig.esm.json",
    "build": "npm run patch && npm run build:puppeteer && npx vite build",
    "ci:tests": "cd tests && npm ci",
    "test:generate:worker": "npm run patch && npm run build:puppeteer && node ./utils/generate_worker_tests.js",
    "test:generate:proxy": "node ./utils/generate_proxy_tests.js",
    "test:deploy": "npm run test:generate:worker && npm run ci:tests && cd tests && npx wrangler deploy",
    "test:dev": "npm run test:generate:worker && cd tests && npx wrangler dev",
    "test:full": "npm run test:generate:proxy && cd tests && npx playwright test",
    "test:smoke": "npm run test:generate:proxy && cd tests && npx playwright test --grep \"@smoke\"",
    "test:merge": "cd tests && npx playwright merge-reports --reporter html ./all-blob-reports"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250723.0",
    "mitt": "^3.0.1",
    "parsel-js": "^1.1.2",
    "rxjs": "^7.8.1",
    "vite": "^6.3.5",
    "wrangler": "4.85.0"
  }
}
