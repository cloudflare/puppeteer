{
  "name": "@cloudflare/browser",
  "private": true,
  "description": "Cloudflare Browser Run packages",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "clean": "npm run clean --workspaces --if-present",
    "diff:playwright": "cd submodules/playwright && git diff HEAD > ../../patches/playwright.patch",
    "patch:playwright": "cd submodules/playwright && git apply --reverse --check ../../patches/playwright.patch 2>/dev/null && echo 'playwright patch already applied' || git apply ../../patches/playwright.patch",
    "diff:puppeteer": "cd submodules/puppeteer && git diff HEAD > ../../patches/puppeteer.patch",
    "patch:puppeteer": "cd submodules/puppeteer && git apply --reverse --check ../../patches/puppeteer.patch 2>/dev/null && echo 'puppeteer patch already applied' || git apply ../../patches/puppeteer.patch",
    "diff": "npm run diff:playwright && npm run diff:puppeteer",
    "patch": "npm run patch:playwright && npm run patch:puppeteer"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260426.1",
    "@tsconfig/node18": "^18.2.6",
    "@types/picomatch": "^4.0.3",
    "picomatch": "^4.0.4",
    "pkg-pr-new": "^0.0.67",
    "vite": "^8.0.10",
    "wrangler": "4.85.0"
  }
}
